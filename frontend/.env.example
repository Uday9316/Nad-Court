# OpenClaw Backend via Tunnel (LIVE AI)
# This uses the tunnel to my AWS server with real OpenClaw
VITE_API_URL=https://chilly-worlds-try.loca.lt

# Fallback: Render backend (pre-generated cases)
# Use this if the tunnel is not available
# VITE_API_URL=https://nad-court.onrender.com

# Local development
# VITE_API_URL=http://localhost:3001
